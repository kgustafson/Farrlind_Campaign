# Trinyvale Database Seed

Place Trinyvale-specific first-boot SQL files here.

The shared database schema and generic lookup rows are loaded from `campaigns/init`.
This directory is only for Trinyvale canon seed data, not shared schema.
